
do double predicat (node[3][2])
do some functions in xpath syntax (lang, string, number, count...)
do unclosed <link> from html document handling
do global parsing error check => return an ERROR
do check memory leaks
do roxml_check_xml for xml and xpath syntax error check
do all function doxygen
add a read XML from fd add a parse stream
add a prev sibling func and a next sibling func
